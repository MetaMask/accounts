# Symlink to a node_modules tree that has playwright + ws.
# Created at run time; see README "How to run".
node_modules

# Build artifacts — generated by build-connect-bundle.cjs at run time.
# (Mirrors Phase 0's trezor-r1/ handling of trezor-connect.cached.js.)
extension/trezor-connect.bundle.js
_entry.js

# Track result logs explicitly (the repo root .gitignore drops *.log;
# the spike outcome are part of the recorded evidence).
!*.log
